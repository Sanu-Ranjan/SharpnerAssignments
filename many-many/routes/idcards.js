const router = require("express").Router();

//list id cards--get request to list all cards

//add id card--post request to add id card

//delete card--delete request

module.exports = {
  router,
};
